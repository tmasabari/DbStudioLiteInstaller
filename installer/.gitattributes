*.manifest binary
*.application binary
*.deploy binary
# git was screwing with the line endings of the ClickOnce deployment files, which apparently causes some hash mismatch and, consequently, a failed installation.
